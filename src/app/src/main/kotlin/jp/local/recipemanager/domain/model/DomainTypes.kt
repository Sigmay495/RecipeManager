package jp.local.recipemanager.domain.model

enum class RecipeCategory { ONE_DISH, MAIN, SIDE, SOUP }

enum class EffortLevel { EASY, NORMAL, HARD }

enum class RecipeStatus { ACTIVE, DELETED }

enum class MenuRole { ONE_DISH, MAIN, SIDE, SOUP }

enum class FoodGroup {
    GRAIN,
    MEAT,
    SEAFOOD,
    EGG,
    DAIRY,
    SOY,
    VEGETABLE,
    MUSHROOM,
    SEAWEED,
    FRUIT,
    FAT,
    SEASONING,
}
